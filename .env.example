# Optional: leave both blank for a clearly labeled local preview.
# For shared comments, run supabase/comments.sql and set BOTH values.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
# Never put a Supabase secret/service-role key or the private trip token here.
# Open the hosted app at /#trip=YOUR_PRIVATE_TOKEN instead.
